import type { Meta, StoryObj } from '@storybook/react';
import ChargeInput from './ChargeInput';

export default {
  title: 'ChargeInput',
  component: ChargeInput,
} as Meta;

type Story = StoryObj<typeof ChargeInput>;

export const Default: Story = {};
